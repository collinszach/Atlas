[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
markers =
    integration: marks tests that require running infrastructure (PostgreSQL, Redis)
addopts = -m "not integration"
